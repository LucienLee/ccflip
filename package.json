{
  "name": "ccflip",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "ccflip": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile ccflip",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.2.1"
  }
}
